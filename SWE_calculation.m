%% This is the function for realizing the algorithm of SWE retrival.

function Z= SWE_Algorithm(EO_BT_info1, EO_BT_info2)

[nscans,npoints,band]=size(EO_BT_info1); 

for i=1:nscans
    for j=1:npoints
        
    end
end
